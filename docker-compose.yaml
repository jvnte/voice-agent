version: '3.8'

services:
  website:
    build:
      context: ./apps/website
      dockerfile: Dockerfile
    container_name: hotel-website
    ports:
      - "3000:80"
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
